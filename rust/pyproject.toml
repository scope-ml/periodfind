[build-system]
requires = ["maturin>=1.0"]
build-backend = "maturin"

[project]
name = "periodfind_cpu"
version = "0.1.0"
requires-python = ">=3.8"

[tool.maturin]
module-name = "periodfind.periodfind_cpu"
